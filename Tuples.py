#!/usr/bin/python

#!creating tuples

coordinates = (5, 8) #!tuples can't be changed or modified
print(coordinates[1])
print(coordinates[0])

#!N.B the difference between tuples and list is that in list we use square brackets"[]" and in tuples we use parenthesis "()"

#!A list of tuples
location = [(8, 6), (9, 7), (10, 8), (11, 9)]
print(location[1])

#!Go ahead and Test it out
